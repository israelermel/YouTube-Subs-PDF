services:
  web:
    build: .
    container_name: youtube-subs-pdf
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      - FLASK_APP=app.py
    command: flask run --host=0.0.0.0 --port=5000